node_modules/
dist/
*.log
*.tsbuildinfo

# Packaged desktop installers (.dmg/.exe/.AppImage) - shipped via GitHub Releases
release/

# Machine-specific runtime state (never commit; regenerated per user)
backend/app-config.json
backend/data/
backend/uploads/
backend/secrets.local.json

# Agent workspace contents (keep the folder, not its files)
workspace/*
!workspace/.gitkeep

.DS_Store
